# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store

# Scraper raw HTML (uploaded as a workflow artifact instead of committed)
snapshots/raw/

# Local output of scripts/check-schema-drift.js — the /data dashboard reads it
# to show when drift was last checked, which is per-machine, not shared state.
snapshots/schema-drift.json