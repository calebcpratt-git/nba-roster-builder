# Two separate keys on purpose — the /data dashboard's Ask GM panel
# (app/api/data-chat, dev-only) and the shipped GM assistant
# (app/api/gm-chat) are billed and rate-limited independently.
DATA_CHAT_ANTHROPIC_API_KEY
GM_CHAT_ANTHROPIC_API_KEY
